<%= form_for(@dupadupa) do |f| %>
  <% if @dupadupa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dupadupa.errors.count, "error") %> prohibited this dupadupa from being saved:</h2>

      <ul>
      <% @dupadupa.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :message %><br />
    <%= f.text_area :message %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
